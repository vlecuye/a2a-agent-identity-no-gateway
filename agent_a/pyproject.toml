[project]
name = "agent-a"
version = "0.1.0"
description = "Coordinator agent in ADK delegating tasks over A2A with Agent Identity"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "google-adk[gcp,otel-gcp,a2a]>=2.8.0,<3.0.0",
    "a2a-sdk[http-server]>=1.0.0,<2.0.0",
    "google-cloud-aiplatform[evaluation,agent-engines]>=1.156.0,<2.0.0",
    "google-cloud-logging>=3.12.0,<4.0.0",
    "google-genai>=0.1.0",
    "httpx>=0.27.0",
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "aiohttp>=3.10.0",
    "pyjwt[crypto]>=2.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
